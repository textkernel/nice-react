const contexts = ['brand', 'link', 'primary', 'accent', 'good', 'warning', 'bad'];
const sizes = ['small', 'medium', 'large'];
 
const niceProps = {
    contexts,
    sizes
};

module.exports = niceProps;
